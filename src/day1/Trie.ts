export default class Trie {




    constructor() {
    }

    insert(item: string): void {
        //curr = head
        //for c in str
        // if curr[c]
        //    curr = curr[c]
        //  else
        //    node = createNode()
        //    curr[c] = node
        //    curr = node
        //curr.isWord = true

}
    delete(item: string): void {

}
    find(partial: string): string[] {

}
}
